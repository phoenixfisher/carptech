# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=carptech
DB_USER=postgres # Change this to your actual PostgreSQL username
DB_PASSWORD=postgres # Change this to your actual PostgreSQL password

# JWT secret — change this to a long random string in production
JWT_SECRET=change_me_to_something_secret

# Express server port (leave as 3001 unless you have a conflict)
PORT=3001
